"use client";
import styled from "styled-components";

export const Wrapper = styled.section`
  position: relative;

  color: var(--grey-70);

  display: grid;
  grid-auto-rows: auto;

  row-gap: 40px;
`;
